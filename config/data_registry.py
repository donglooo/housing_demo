# Data Registry Configuration
# This file tracks which data files are disabled (hidden) from the Playground.
# Uses a blacklist approach: All files are enabled by default unless listed here.

disabled_files = ['稅籍_coded_202602041056.parquet', '稅電_coded_202602041000.parquet']
